# ╔══════════════════════════════════════════════════════════════╗
# ║  OriginA — Production Environment Variables                 ║
# ║  Copy to .env and server/.env, fill in real values          ║
# ╚══════════════════════════════════════════════════════════════╝

# ─── Server ─────────────────────────────────────────────────────
PORT=3001                                          # Server port
NODE_ENV=production                                # REQUIRED: must be "production"
LOG_LEVEL=warn                                     # info | warn | error
CORS_ORIGINS=https://origina.io                    # Comma-separated allowed origins

# ─── Authentication ─────────────────────────────────────────────
# ⚠️  REQUIRED — generate with: openssl rand -base64 64
JWT_SECRET=
# Optional — API key for programmatic access
ORIGINA_API_KEY=

# ─── PostgreSQL ─────────────────────────────────────────────────
# REQUIRED — all fields needed for production
PG_HOST=localhost                                  # Database host
PG_PORT=5432                                       # Database port
PG_DATABASE=origina                                # Database name
PG_USER=origina                                    # Database user
PG_PASSWORD=                                       # ⚠️  REQUIRED — set a strong password

# ─── Redis ──────────────────────────────────────────────────────
# Option A: Individual connection fields
REDIS_HOST=localhost                               # Redis host
REDIS_PORT=6379                                    # Redis port
REDIS_USERNAME=                                    # Redis ACL username (Redis Cloud requires this)
REDIS_PASSWORD=                                    # Redis password
REDIS_DB=0                                         # Redis database number
REDIS_PREFIX=oa:                                   # Key prefix for namespacing
REDIS_TLS=false                                    # Set to "true" for Redis Cloud / TLS connections

# Option B: Full connection URL (overrides Option A if set)
# REDIS_URL=rediss://default:password@host:port    # Use rediss:// for TLS, redis:// for plain

# ─── LLM Providers ─────────────────────────────────────────────
# Set at least one. Anthropic recommended as primary.
ANTHROPIC_API_KEY=                                 # ⚠️  REQUIRED — primary LLM
OPENAI_API_KEY=                                    # Optional — fallback / embeddings
GOOGLE_API_KEY=                                    # Optional
DEEPSEEK_API_KEY=                                  # Optional
MISTRAL_API_KEY=                                   # Optional
GROQ_API_KEY=                                      # Optional
OPENROUTER_API_KEY=                                # Optional
XAI_API_KEY=                                       # Optional
MINIMAX_API_KEY=                                   # Optional

# ─── Voice (Optional) ──────────────────────────────────────────
DEEPGRAM_API_KEY=                                  # Speech-to-Text (Deepgram Nova)
ELEVENLABS_API_KEY=                                # Text-to-Speech (ElevenLabs)

# ─── Search (Optional) ─────────────────────────────────────────
SEARCH_API_KEY=                                    # Web search for AI agents

# ─── Channel Connectors (Optional) ────────────────────────────
TELEGRAM_BOT_TOKEN=                                # Telegram bot token (from @BotFather)

# ─── Vite (Frontend) ───────────────────────────────────────────
VITE_API_URL=https://origina.io                    # API base URL
VITE_WS_URL=wss://origina.io                       # WebSocket URL
